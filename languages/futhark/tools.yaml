schema: 1
language: futhark
provenance:
  authored: '2026-09-22'
  basis: primary documentation for the language; entries under verify are from prior knowledge and must be confirmed against
    docs before a task relies on them
  verify:
  - futhark fmt
  - futhark lsp
  none_means: no established tool is known for this role; do not invent one
authority:
  compiler_or_runtime: futhark (c/multicore/cuda/opencl backends)
  package_manager: futhark pkg
  formatter: futhark fmt
  lsp: futhark lsp
  debugger: futhark repl
  profiler: futhark bench
  test: futhark test
  fuzz: none
  mutation: none
  security: none
  docs: https://futhark.readthedocs.io/
  research: https://futhark-lang.org/blog.html
profiles:
  implementation:
  - futhark pkg
  - futhark fmt
  - futhark lsp
  - futhark test
  debugging:
  - futhark repl
  - futhark test
  endpoint:
  - futhark test
  security:
  - futhark test
  performance:
  - futhark bench
  - futhark test
  reliability:
  - futhark test
  polyglot:
  - schema_or_abi
  - futhark test
policy:
  default_tools:
  - futhark (c/multicore/cuda/opencl backends)
  - futhark fmt
  - futhark test
  optional_tools:
  - futhark repl
  - futhark bench
  avoid_by_default:
  - duplicate_linters
  - tools_not_in_this_manifest_without_a_recorded_reason
  warnings: non_blocking
  blockers:
  - compile_failure
  - test_failure
  - security_failure
