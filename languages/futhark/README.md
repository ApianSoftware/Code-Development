# Futhark

Purpose: data-parallel functional computing for compute-intensive GPU/CPU kernels.

Futhark is an ongoing research project and is designed for focused compute-intensive components rather than whole applications.

Best pattern: host application -> Futhark kernel -> accelerator/CPU -> typed result.

Official: https://futhark-lang.org/