{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/ApianSoftware/Code-Development/main/tools/tools.schema.json",
  "title": "Language tool manifest",
  "description": "The ONE declaration of what languages/<route>/tools.yaml may contain. Three readers: scripts/atlas.py (fails the contract on a manifest that does not conform), scripts/packprobe.py (parses each entry's kind from this grammar instead of guessing it), and languages/PACK-TOOLS-SPEC.md (whose skeleton is generated from this file). WHY IT EXISTS: the roles below are single-purpose fields that also held prose, so 49% of declared entries could not be evaluated by any instrument and the packs were unverifiable by construction. An entry now declares its KIND instead of leaving a probe to infer it from punctuation. BLIND SPOT: this file constrains the SHAPE of an entry, not its truth. `EXPLAIN ANALYZE` and `go test` are the same shape; only the author knows which one PATH can find, which is why builtin:/lib:/concept: are written by hand and provenance.verify still carries the confirmation debt.",
  "type": "object",
  "required": [
    "schema",
    "language",
    "provenance",
    "authority",
    "profiles",
    "policy"
  ],
  "additionalProperties": false,
  "properties": {
    "schema": {
      "const": 1,
      "description": "Manifest format version. Bumped only by a change existing readers must handle."
    },
    "language": {
      "type": "string",
      "pattern": "^[a-z0-9]+$",
      "description": "The pack's own basename, so a manifest copied into another pack fails the contract."
    },
    "provenance": {
      "type": "object",
      "required": [
        "authored",
        "basis",
        "verify",
        "none_means"
      ],
      "additionalProperties": false,
      "description": "Where the entries came from and what is still unconfirmed. A manifest with no provenance reads as measured when it was recalled.",
      "properties": {
        "authored": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
        },
        "basis": {
          "type": "string",
          "minLength": 1
        },
        "verify": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Entries to confirm against the language's own documentation before a task relies on them. Empty means nothing is outstanding, never that nothing was ever doubted."
        },
        "none_means": {
          "type": "string",
          "minLength": 1
        }
      }
    },
    "authority": {
      "type": "object",
      "required": [
        "compiler_or_runtime",
        "package_manager",
        "formatter",
        "lsp",
        "debugger",
        "profiler",
        "test",
        "fuzz",
        "mutation",
        "security",
        "docs",
        "research"
      ],
      "additionalProperties": false,
      "description": "One role, one authority. Native toolchain first; MCP and AI layers are adapters, never authorities.",
      "properties": {
        "compiler_or_runtime": {
          "$ref": "#/$defs/entryOrList"
        },
        "package_manager": {
          "$ref": "#/$defs/entryOrList"
        },
        "formatter": {
          "$ref": "#/$defs/entryOrList"
        },
        "lsp": {
          "$ref": "#/$defs/entryOrList"
        },
        "debugger": {
          "$ref": "#/$defs/entryOrList"
        },
        "profiler": {
          "$ref": "#/$defs/entryOrList"
        },
        "test": {
          "$ref": "#/$defs/entryOrList"
        },
        "fuzz": {
          "$ref": "#/$defs/entryOrList"
        },
        "mutation": {
          "$ref": "#/$defs/entryOrList"
        },
        "security": {
          "$ref": "#/$defs/entryOrList"
        },
        "docs": {
          "$ref": "#/$defs/url"
        },
        "research": {
          "$ref": "#/$defs/url"
        }
      }
    },
    "profiles": {
      "type": "object",
      "required": [
        "implementation",
        "debugging",
        "endpoint",
        "security",
        "performance",
        "reliability",
        "polyglot"
      ],
      "additionalProperties": false,
      "description": "What a task activates. An empty list is a real answer: this pack has nothing established for that task.",
      "properties": {
        "implementation": {
          "$ref": "#/$defs/entryList"
        },
        "debugging": {
          "$ref": "#/$defs/entryList"
        },
        "endpoint": {
          "$ref": "#/$defs/entryList"
        },
        "security": {
          "$ref": "#/$defs/entryList"
        },
        "performance": {
          "$ref": "#/$defs/entryList"
        },
        "reliability": {
          "$ref": "#/$defs/entryList"
        },
        "polyglot": {
          "$ref": "#/$defs/entryList"
        }
      }
    },
    "policy": {
      "type": "object",
      "required": [
        "default_tools",
        "optional_tools",
        "avoid_by_default",
        "warnings",
        "blockers"
      ],
      "additionalProperties": false,
      "properties": {
        "default_tools": {
          "$ref": "#/$defs/entryList",
          "description": "Activated without being asked for. Capped by MAX_DEFAULT_TOOLS in atlas.py: a manifest that defaults to everything is not a bounded surface."
        },
        "optional_tools": {
          "$ref": "#/$defs/entryList"
        },
        "avoid_by_default": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9_]*$"
          },
          "description": "Habits this pack refuses. Policy words, not tool names — a manifest that names nothing to avoid has an unbounded surface."
        },
        "warnings": {
          "enum": [
            "non_blocking",
            "blocking"
          ]
        },
        "blockers": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9_]*_failure$"
          },
          "description": "What fails a change in this language. Named conditions, never tools."
        }
      }
    },
    "notes": {
      "type": "object",
      "additionalProperties": {
        "type": "string",
        "minLength": 1
      },
      "propertyNames": {
        "enum": [
          "compiler_or_runtime",
          "package_manager",
          "formatter",
          "lsp",
          "debugger",
          "profiler",
          "test",
          "fuzz",
          "mutation",
          "security",
          "implementation",
          "debugging",
          "endpoint",
          "performance",
          "reliability",
          "polyglot"
        ]
      },
      "description": "WHERE PROSE LIVES. Keyed by the role or profile it qualifies, so `carbon` can stay a probeable entry while 'experimental, built with bazel' stays readable. Never the place for an entry itself."
    }
  },
  "$defs": {
    "entry": {
      "type": "string",
      "pattern": "^(none|(lib|builtin|concept):\\S.*|[A-Za-z0-9][A-Za-z0-9._+-]*(\\|[A-Za-z0-9][A-Za-z0-9._+-]*)*( [-A-Za-z0-9._=:+]+)*)$",
      "description": "A DECLARED ENTRY. Its KIND is written by the author, not inferred by a reader: the command form is deliberately narrow (no parentheses, no slashes, no free words), because that is exactly how prose used to enter a field an instrument then had to skip. x-kinds below is the roster, and every reader — atlas.py, packprobe.py, PACK-TOOLS-SPEC.md — reads it from here.",
      "x-kinds": {
        "command": {
          "marker": "",
          "example": "go test",
          "means": "the first word is a name PATH can resolve; `|` separates equivalent alternatives (`lldb|gdb`)"
        },
        "lib": {
          "marker": "lib:",
          "example": "lib:hypothesis",
          "means": "a package or crate reachable only through its ecosystem, which PATH cannot answer for"
        },
        "builtin": {
          "marker": "builtin:",
          "example": "builtin:EXPLAIN ANALYZE",
          "means": "a facility inside the language, runtime or editor; there is no binary to find"
        },
        "concept": {
          "marker": "concept:",
          "example": "concept:schema-or-ABI boundary",
          "means": "a technique or a boundary rather than a tool"
        },
        "none": {
          "marker": "none",
          "example": "none",
          "means": "no established tool is known for this role; do not invent one"
        }
      }
    },
    "entryList": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/entry"
      }
    },
    "entryOrList": {
      "oneOf": [
        {
          "$ref": "#/$defs/entry"
        },
        {
          "type": "array",
          "minItems": 2,
          "items": {
            "$ref": "#/$defs/entry"
          }
        }
      ],
      "description": "A list means a role genuinely needs several tools TOGETHER (`[ruff, bandit]`). It replaced `ruff+bandit`, where a probe read `+` as `or` and reported a role covered that was half covered."
    },
    "url": {
      "type": "string",
      "pattern": "^https://\\S+$",
      "description": "Primary documentation. https only — a doc link that can be intercepted is not a source of authority."
    }
  }
}
