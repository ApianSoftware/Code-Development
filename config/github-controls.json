{
  "_why": "The DECLARED state of the whole GitHub surface. scripts/ghaudit.py compares this to the live API and exits non-zero on a difference. Before this file that state was typed into SECURITY.md with a date beside it: true when written, unfalsifiable later, stale without saying so. A declaration a machine can compare is the only kind that cannot rot quietly \u2014 and the reverse direction matters most, because it is what catches a webhook, an environment or a merge setting added through the UI and recorded nowhere.",
  "repository": "ApianSoftware/Code-Development",
  "visibility": "public",
  "default_branch": "main",
  "license_spdx": "MIT",
  "private_vulnerability_reporting": "enabled",
  "topics": [
    "agent-tooling",
    "ai-agents",
    "code-quality",
    "data-science",
    "developer-tools",
    "engineering-atlas",
    "github-actions",
    "llm",
    "mcp",
    "openssf",
    "polyglot",
    "quantum-computing",
    "software-engineering",
    "static-analysis",
    "supply-chain-security",
    "verification"
  ],
  "security_and_analysis": {
    "secret_scanning": "enabled",
    "secret_scanning_push_protection": "enabled",
    "secret_scanning_non_provider_patterns": {
      "want": "enabled",
      "accept": "disabled",
      "blocked_by": "MEASURED at v1.3.0: PATCH /repos/{repo} with this field returns HTTP 200 and changes nothing, and /user/code-security/configurations returns 404 for this account. The toggle belongs to GitHub Secret Protection, which this user-owned repository does not carry (security_and_analysis.advanced_security is null). Re-measure with `python scripts/ghaudit.py`; it becomes a DIFF the moment the platform grants it."
    },
    "secret_scanning_validity_checks": {
      "want": "enabled",
      "accept": "disabled",
      "blocked_by": "Same gate as non-provider patterns, measured the same way at v1.3.0: a 200 that does nothing. Not a blind spot and not an oversight \u2014 a platform entitlement this account does not have."
    },
    "dependabot_security_updates": "enabled"
  },
  "ruleset": {
    "name": "main-protection",
    "enforcement": "active",
    "rules": [
      "deletion",
      "non_fast_forward",
      "required_linear_history",
      "pull_request",
      "required_status_checks"
    ],
    "required_status_checks": [
      "Analyze (actions)",
      "Analyze (python)",
      "Contract",
      "Dependency Review"
    ],
    "_checks_note": "These are CHECK RUN names \u2014 a job's `name:` \u2014 not workflow names. A ruleset naming a check that never reports blocks every merge, so `Atlas CI` (the workflow) would wedge the branch while `Contract` (its job) gates it. MEASURED at v1.3.0 on pull request #2: Contract, Dependency Review, Analyze (python) and Analyze (actions) all reported and passed, which is the evidence for requiring the two CodeQL contexts. The failure on pull request #1 came from a stale branch carrying a deleted advanced CodeQL workflow, not from default setup.",
    "_not_required_yet": {
      "Supply-chain hygiene": "OpenSSF Scorecard runs on push and on a schedule, never on a pull request, so it cannot be a merge gate. Requiring it would name a context that never reports."
    }
  },
  "settings": {
    "delete_branch_on_merge": true,
    "allow_merge_commit": false,
    "allow_squash_merge": true,
    "allow_rebase_merge": true,
    "has_wiki": false,
    "has_pages": false,
    "_settings_note": "allow_merge_commit is FALSE because the ruleset requires linear history: a merge method the UI offers and the rules refuse is an affordance that produces a failed push and no explanation. delete_branch_on_merge is TRUE because a finished lane with ahead=0 is debris by this repository's own worktree rules. has_wiki is FALSE because the wiki lives in-tree at wiki/, under the same contract as everything else \u2014 a second, unversioned wiki is a duplicate surface where one copy goes stale and no reader can tell which is live."
  },
  "webhooks": {
    "count": 0,
    "_webhooks_note": "Zero, and checked in both directions. A webhook is an outbound data path: one added through the UI and recorded nowhere is the shape this audit exists to catch."
  },
  "environments": {
    "names": [
      "Developer Playground",
      "copilot"
    ],
    "secrets_allowed": 0,
    "_environments_note": "Both were created by GitHub features rather than by a deployment, and both hold zero secrets and zero variables \u2014 verified per environment, because an environment is exactly where a secret hides from a repository-level scan. This repository deploys nothing, so an environment that acquires a secret is a finding, not a configuration."
  },
  "scorecard": {
    "minimum": 5.9,
    "check_floors": {
      "Binary-Artifacts": 10,
      "Branch-Protection": 3,
      "CI-Tests": 10,
      "CII-Best-Practices": 0,
      "Code-Review": 0,
      "Contributors": 0,
      "Dangerous-Workflow": 10,
      "Dependency-Update-Tool": 10,
      "Fuzzing": 0,
      "License": 10,
      "Maintained": 0,
      "Pinned-Dependencies": 0,
      "SAST": 10,
      "Security-Policy": 4,
      "Token-Permissions": 10,
      "Vulnerabilities": 10
    },
    "_scorecard_note": "A RATCHET, AND IT IS PER CHECK. The aggregate hides which check fell: Branch-Protection can drop three points while Pinned-Dependencies rises three and the total does not move, so a single floor would have reported that as fine. Every check carries its own floor, measured at contract v2.0.0, and ghaudit reports each one that is below it. Raising a floor must name what earned the rise. docs/CERTIFICATION.md carries, per check, what would raise it and whether that is owner action or structural. Checks scoring -1 (Packaging, Signed-Releases) are INCONCLUSIVE rather than failing \u2014 nothing is published to a package index \u2014 so they carry no floor and are named in the document instead of being quietly counted as passes."
  },
  "releases": {
    "every_tag_has_a_release": true,
    "_releases_note": "A version with no tag is a claim with no artifact; a tag with no release is an artifact nobody can find. The release procedure in docs/VERSIONING.md ends here."
  },
  "description_is_countless": true,
  "_description_note": "The GitHub description carries NO count. It said '29 language routes' while the atlas routed 34 \u2014 the same defect as a number typed into prose, on the surface a reader sees before the README."
}
